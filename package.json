{
  "name": "noza-app",
  "version": "1.0.0",
  "description": "Application Noza complète",
  "scripts": {
    "start": "cd backend && npm start",
    "build": "cd backend && npm install",
    "dev": "cd backend && npm run dev"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
